✅ Converted application.properties (environment-variable version)
# ==========================================
# = APPLICATION CONFIG
# ==========================================

spring.application.name=${APP_NAME:AuthSync}
server.port=${SERVER_PORT:8081}

# Optional: base path (request prefix)
server.servlet.context-path=${SERVER_CONTEXT_PATH:/api/s-1}

# ==========================================
# = DATABASE CONFIGURATION
# ==========================================

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:authofy}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:system123}

spring.jpa.hibernate.ddl-auto=${HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SHOW_SQL:true}

# ==========================================
# = JWT CONFIGURATION
# ==========================================

jwt.token.secret=${JWT_SECRET:thisisjwttokensecretwhichwillgeneratetokenandmakeapplicationsecure}

# ==========================================
# = MAIL SERVER CONFIGURATION
# ==========================================

spring.mail.host=${MAIL_HOST:smtp-relay.brevo.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:9266b4001@smtp-brevo.com}
spring.mail.password=${MAIL_PASSWORD:DGLCsTEw94vM5Nh6}
spring.mail.protocol=${MAIL_PROTOCOL:smtp}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:true}
spring.mail.properties.mail.smtp.from=${MAIL_FROM:mohadarif9211@gmail.com}


🐳 Example Docker Compose (matching these variables)

version: "3.8"

services:
  mysql:
    image: mysql:8.0
    container_name: mysql-db
    environment:
      MYSQL_ROOT_PASSWORD: system123
      MYSQL_DATABASE: authofy
      MYSQL_USER: root
      MYSQL_PASSWORD: system123
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql

  app:
    build: .
    container_name: authsync-app
    depends_on:
      - mysql
    ports:
      - "8081:8081"
    environment:
      APP_NAME: AuthSync
      SERVER_PORT: 8081
      SERVER_CONTEXT_PATH: /api/s-1

      DB_HOST: mysql
      DB_PORT: 3306
      DB_NAME: authofy
      DB_USER: root
      DB_PASSWORD: system123
      HIBERNATE_DDL_AUTO: update
      SHOW_SQL: true

      JWT_SECRET: thisisjwttokensecretwhichwillgeneratetokenandmakeapplicationsecure

      MAIL_HOST: smtp-relay.brevo.com
      MAIL_PORT: 587
      MAIL_USERNAME: 9266b4001@smtp-brevo.com
      MAIL_PASSWORD: DGLCsTEw94vM5Nh6
      MAIL_PROTOCOL: smtp
      MAIL_SMTP_AUTH: true
      MAIL_SMTP_STARTTLS: true
      MAIL_FROM: mohadarif9211@gmail.com

volumes:
  mysql-data:



✅ Docker Image creation in Dockerfile
# 1st way

# Use a lightweight base image
FROM openjdk:17-jdk-slim

# Set working directory
WORKDIR /app

# Copy the JAR file into the container
COPY target/*.jar app.jar

# Expose port 8080
EXPOSE 8080

# Run the JAR file
ENTRYPOINT ["java", "-jar", "app.jar"]

# 2nd way

FROM openjdk:17-jdk-slim

COPY target/*.jar AuthSync-0.0.1-SNAPSHOT.jar

EXPOSE 8082

ENTRYPOINT ["java","-jar","AuthSync-0.0.1-SNAPSHOT.jar"]


** notes:- now run mysql container as per above version then your application will
           properly with database